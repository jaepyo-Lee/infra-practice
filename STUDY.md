## vpc
aws에서 리소스를 생성하는 큰 범위
ip를 가장 큰 단위로 하지 않는 이유는 고도화될수록 vpc peering과 같이 서로 다른 vpc간에 연결이 필요할때 cidr이 겹치면 안되는데, 너무크면 cidr이 겹칠수 있는 확률이 높기 때문

vpc가 다른데, 구분을 못하는 이유
VPC는 논리적으로는 “서로 분리된 공간”이 맞는데, 두 VPC를 “연결(피어링/TGW/VPN)”하려는 순간에는 네트워크 장비(라우터)가 IP만 보고 목적지를 결정해야 해서, IP 대역이 겹치면 어디로 보내야 할지 결정할 수 없게 돼.

즉 “구분을 못한다”는 말은 라우팅 관점에서 하는 말이야.

VPC가 분리돼도, 라우팅은 ‘목적지 IP’만 보고 결정해

겹치면 목적지가 어디인지 알지 못함.


